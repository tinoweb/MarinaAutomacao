# Sobrescreve configuracoes para desenvolvimento local
# Este arquivo e lido automaticamente pelo docker-compose junto com docker-compose.yml

services:
  app:
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
      - /app/.venv
